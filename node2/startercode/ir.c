#include "ir.h"
#include "../sam/sam3x/include/sam.h"

/**/
void ir_count_score(uint8_t *score){ //Create a function that is able to count the score. This will later be used for the game application
    uint16_t data = adc_data(); //hva kommer ut her, må den endres litt?
    if (data...){ //count++
        score++;
    }
}

